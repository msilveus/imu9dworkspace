[package]
name = "lsm6dsox"
version = "0.1.0"
edition = "2024"

[dependencies]
embedded-hal = "1.0.0"
bitflags = "2.9.1"
heapless = "0.8"
paste = "1.0.15"
log = "0.4.27"
rtt-target = "0.6.1"

[lib]
crate-type = ["lib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
